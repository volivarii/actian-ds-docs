---
// ComponentTabs.astro — stub placeholder until Task 4 implements the full
// tab strip. Accepts component + activeTab props but renders nothing yet.
interface Props {
  component: string;
  activeTab: string;
}
const { component, activeTab } = Astro.props;
---
<!-- ComponentTabs: {component} / {activeTab} — full implementation in Task 4 -->
