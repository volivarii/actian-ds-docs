---
import { renderVariantMatrix } from "../lib/variant-matrix.mjs";

interface Props {
  variantAxes: Array<{ axis: string; values: string[] }>;
}
const { variantAxes } = Astro.props;

const html = renderVariantMatrix(variantAxes);
---

<Fragment set:html={html} />
