---
title: Content guidelines
description: Voice, terminology, UI copy patterns — Actian DS content.
---

import StarlightPage from "@astrojs/starlight/components/StarlightPage.astro";
import PageMetadata from "../components/PageMetadata.astro";
import contentMd from "../../vendor/content/dist/content.md?raw";
import { Marked } from "marked";
import customHeadingId from "marked-custom-heading-id";

export const renderMd = (text) => new Marked().use(customHeadingId()).parse(text);

<StarlightPage frontmatter={{ title: "Content guidelines", description: "Voice, terminology, UI copy patterns — Actian DS content." }}>
  <PageMetadata
    slug="content"
    source="content/dist/content.md"
    schema={1}
  />
  <Fragment set:html={renderMd(contentMd)} />
</StarlightPage>
