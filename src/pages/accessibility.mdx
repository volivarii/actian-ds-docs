---
title: Accessibility
description: WCAG 2.2 AA guidance for the Actian DS.
---

import StarlightPage from "@astrojs/starlight/components/StarlightPage.astro";
import PageMetadata from "../components/PageMetadata.astro";
import a11yContent from "../../vendor/accessibility/accessibility.md?raw";
import { Marked } from "marked";
import customHeadingId from "marked-custom-heading-id";

export const renderMd = (text) => new Marked().use(customHeadingId()).parse(text);

<StarlightPage frontmatter={{ title: "Accessibility", description: "WCAG 2.2 AA guidance for the Actian DS." }}>
  <PageMetadata
    slug="accessibility"
    source="accessibility/accessibility.md"
    schema={1}
  />
  <Fragment set:html={renderMd(a11yContent)} />
</StarlightPage>
