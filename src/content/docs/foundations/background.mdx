---
title: Background
description: Background surface tokens — default, subtle, muted, emphasis, and feedback-state backgrounds for the Actian DS.
sidebar:
  order: 1
---

import TokenTable from "../../../components/TokenTable.astro";
import PageMetadata from "../../../components/PageMetadata.astro";
import backgrounds from "../../../../vendor/foundations/dist/tokens/backgrounds.json";

<PageMetadata
  slug="foundations.background"
  source="foundations/dist/tokens/backgrounds.json"
  schema={1}
/>

Background surface tokens for the Actian DS. Each row carries its CSS variable name (the primitive token identifier) alongside the resolved value and a visual swatch.

{(backgrounds.blocks || []).filter((b) => b.type === "table").map((b) => (
  <TokenTable headers={b.headers} rows={b.rows} showSwatch={true} />
))}
