---
title: Brand & feedback colors
description: Brand scales (primary / neutral) and feedback scales (error / success / warning) for the Actian DS.
sidebar:
  order: 4
---

import TokenTable from "../../../components/TokenTable.astro";
import TokenScale from "../../../components/TokenScale.astro";
import PageMetadata from "../../../components/PageMetadata.astro";
import dtcg from "../../../../vendor/tokens/tokens.json";
import themePalettes from "../../../../vendor/foundations/dist/tokens/global-color/theme-palettes.json";

<PageMetadata
  slug="foundations.brand-feedback-colors"
  source="foundations/dist/tokens/global-color/"
  schema={1}
/>

The Actian DS brand and feedback color scales. Concrete hex values vary per theme; the token name is the stable identifier.

## Themes

{themePalettes.body && <p>{themePalettes.body}</p>}

{(themePalettes.blocks || []).filter((b) => b.type === "table").map((b) => (
  <TokenTable headers={b.headers} rows={b.rows} />
))}

## Brand & status scales

Full primary / neutral / error / success / warning color ramps. Each row carries its CSS variable name (the primitive token identifier).

### Primary

<TokenScale data={dtcg.color.primary} prefix="color-primary" showValue={false} />

### Neutral

<TokenScale data={dtcg.color.neutral} prefix="color-neutral" showValue={false} />

### Error

<TokenScale data={dtcg.color.error} prefix="color-error" showValue={false} />

### Success

<TokenScale data={dtcg.color.success} prefix="color-success" showValue={false} />

### Warning

<TokenScale data={dtcg.color.warning} prefix="color-warning" showValue={false} />

### Annotation

<TokenScale data={dtcg.color.annotation} prefix="color-annotation" showValue={false} />
