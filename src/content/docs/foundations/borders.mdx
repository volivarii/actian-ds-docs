---
title: Borders
description: Border tokens from the Actian foundations.
---

import TokenScale from "../../../components/TokenScale.astro";
import PageMetadata from "../../../components/PageMetadata.astro";
import dtcg from "../../../../vendor/tokens/tokens.json";

<PageMetadata
  slug="foundations.borders"
  source="foundations/dist/tokens/borders/"
  schema={1}
/>

Border tokens for the Actian DS — radius, width, and border colors.

## Radius

<TokenScale data={dtcg.border.radius} prefix="border-radius" />

## Width

<TokenScale data={dtcg.border.width} prefix="border-width" />

## Border colors

Semantic border-color tokens used across surfaces, fields, and states.

<TokenScale
  data={Object.fromEntries(Object.entries(dtcg.border).filter(([k]) => !["radius", "width"].includes(k)))}
  prefix="border"
  visualKind="color"
/>
