---
title: Feedback
description: "Status regions and live announcements — toasts, banners, inline alerts — that report system state to the user."
---

import PageMetadata from "../../../../components/PageMetadata.astro";
import ComponentCard from "../../../../components/ComponentCard.astro";
import dskit from "../../../../../vendor/components/dist/registries/dskit.json";

<PageMetadata
  slug="components.feedback"
  source="components/dist/categories/feedback-defaults.json"
  schema={1}
/>

<div class="component-card-grid">
  {Object.entries(dskit.components)
    .filter(([slug, e]) => e.category === "Feedback")
    .map(([slug, e]) => (
      <ComponentCard slug={slug} name={e.name || slug} category={e.category} categorySlug="feedback" />
    ))}
</div>
