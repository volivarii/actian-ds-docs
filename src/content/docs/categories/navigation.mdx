---
title: Navigation
description: Landmark regions and wayfinding controls — nav bars, sidebars, tabs, breadcrumbs — that orient and move users between places.
---

import Anatomy from "../../../components/Anatomy.astro";
import VariantMatrix from "../../../components/VariantMatrix.astro";
import MotionPattern from "../../../components/MotionPattern.astro";
import AccessibilityRefs from "../../../components/AccessibilityRefs.astro";
import PageMetadata from "../../../components/PageMetadata.astro";
import defaults from "../../../../vendor/components/dist/categories/navigation-defaults.json";

<PageMetadata
  slug="categories.navigation"
  source="components/dist/categories/navigation-defaults.json"
  schema={1}
/>

**Confidence:**
{Object.entries(defaults.confidence || {}).map(([k, v]) => (
  <span><code>{k}</code>: <strong>{v}</strong>&nbsp;&nbsp;</span>
))}

## Anatomy

<Anatomy parts={defaults.card_anatomy.parts} />

## Variants

<VariantMatrix variantAxes={defaults.card_component.variantAxes} />

## Motion

<MotionPattern patternRefs={defaults.card_motion.patternRefs} />

## Accessibility

<AccessibilityRefs requirementRefs={defaults.card_accessibility.requirementRefs} />

## Design rationale

See [`vendor/components/dist/categories/navigation.md`](https://github.com/volivarii/actian-ds-knowledge/blob/main/components/src/categories/navigation.md) for the full design rationale.
