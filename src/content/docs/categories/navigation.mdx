---
title: Navigation
description: "Landmark regions and wayfinding controls — nav bars, sidebars, tabs, breadcrumbs — that orient and move users between places."
---

import PageMetadata from "../../../components/PageMetadata.astro";
import ComponentCard from "../../../components/ComponentCard.astro";
import dskit from "../../../../vendor/components/dist/registries/dskit.json";

<PageMetadata
  slug="categories.navigation"
  source="components/dist/categories/navigation-defaults.json"
  schema={1}
/>

<div class="component-card-grid">
  {Object.entries(dskit.components)
    .filter(([slug, e]) => e.category === "Navigation")
    .map(([slug, e]) => (
      <ComponentCard slug={slug} name={e.name || slug} category={e.category} categorySlug="navigation" />
    ))}
</div>
