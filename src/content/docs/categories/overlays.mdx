---
title: Overlays
description: "Surfaces layered above the page — dialogs, popovers, drawers, tooltips — triggered by an element and dismissible."
---

import PageMetadata from "../../../components/PageMetadata.astro";
import ComponentCard from "../../../components/ComponentCard.astro";
import dskit from "../../../../vendor/components/dist/registries/dskit.json";

<PageMetadata
  slug="categories.overlays"
  source="components/dist/categories/overlays-defaults.json"
  schema={1}
/>

<div class="component-card-grid">
  {Object.entries(dskit.components)
    .filter(([slug, e]) => e.category === "Overlays")
    .map(([slug, e]) => (
      <ComponentCard slug={slug} name={e.name || slug} category={e.category} categorySlug="overlays" />
    ))}
</div>
