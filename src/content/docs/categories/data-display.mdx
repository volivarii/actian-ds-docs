---
title: Data Display
description: "Surfaces that present structured information — tables, lists, cards, metrics — without driving primary actions."
---

import PageMetadata from "../../../components/PageMetadata.astro";
import ComponentCard from "../../../components/ComponentCard.astro";
import dskit from "../../../../vendor/components/dist/registries/dskit.json";

<PageMetadata
  slug="categories.data-display"
  source="components/dist/categories/data-display-defaults.json"
  schema={1}
/>

<div class="component-card-grid">
  {Object.entries(dskit.components)
    .filter(([slug, e]) => e.category === "Data Display")
    .map(([slug, e]) => (
      <ComponentCard slug={slug} name={e.name || slug} category={e.category} categorySlug="data-display" />
    ))}
</div>
